<Window x:Class="LCPReportingSystem.View.Setting"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LCPReportingSystem.View"
        xmlns:materialdesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title=" Parameters Setting" Height="210" Width="565" WindowStyle="None" WindowStartupLocation="CenterScreen">
    <Grid >

        <Grid.RowDefinitions>
            <RowDefinition Height="35"/>
            <RowDefinition Height="135"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Margin="0,0,0,0" Height="35" Background="#0064C1" Width="540" Orientation="Horizontal" >
            <TextBlock   Foreground="White" Text="SUBSYSTEM PARAMETRS DATA" Width="220"  Margin="175,0,0,0" Padding="0,5,0,0" FontSize="15" FontWeight="DemiBold"></TextBlock>

        </StackPanel>

        <Border x:Name="stackbtns" Width="540"  Grid.Row="1" VerticalAlignment="Top" Margin="0,0,0,0"  BorderBrush="Black"  BorderThickness="1">
            <StackPanel   Height="100" Orientation="Horizontal"   VerticalAlignment="Top" Margin="0,20,0,0"  >
                <RadioButton  x:Name="rdoDieselgenerator" Command="{Binding DgInfoCommand}" CommandParameter="{Binding RelativeSource={RelativeSource Self}}"  Content="Diesel Generator"  Margin="20,10,0,0"  FontSize="14"></RadioButton>
                <RadioButton  x:Name="rdoUPS" Content="UPS" Command="{Binding RadioInfoCommand}" CommandParameter="{Binding RelativeSource={RelativeSource Self}}" Margin="40,10,0,0" FontSize="14"></RadioButton>
                <RadioButton  x:Name="rdoSwitch" Content="Switch" Command="{Binding RadioInfoCommand}" CommandParameter="{Binding RelativeSource={RelativeSource Self}}" Margin="40,10,0,0" FontSize="14"></RadioButton>
                <RadioButton  x:Name="rdoRadio" Content="Radio" Command="{Binding RadioInfoCommand}" CommandParameter="{Binding RelativeSource={RelativeSource Self}}" Margin="40,10,0,0" FontSize="14"></RadioButton>
                <RadioButton  x:Name="rdoRouter" Content="Router" Visibility="Collapsed" Command="{Binding RadioInfoCommand}" CommandParameter="{Binding RelativeSource={RelativeSource Self}}" Margin="20,10,0,0" FontSize="12"></RadioButton>
            </StackPanel>
        </Border>
        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Right" Margin="0,-40,0,0">
            <Button  x:Name="btnSync" Width="90" Height="29" Content="Sync" Margin="0,0,0,0" Command="{Binding LinegraphSyncInfoCommand}" Style="{StaticResource btnFilterComman}"></Button>
            <Button  x:Name="btnCancel" Content="Cancel"  Width="90" Height="29" Margin="0,0,0,0" Command="{Binding LinegraphCancelCommand}" Style="{StaticResource btnFilterComman}"></Button>
        </StackPanel>
       
              
    </Grid>
</Window>
