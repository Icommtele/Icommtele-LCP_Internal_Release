<UserControl x:Class="LCPReportingSystem.UserControls.PasswordUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LCPReportingSystem.UserControls"
             xmlns:materialdesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <materialdesign:PackIcon x:Name="eyeIcon" MouseLeftButtonDown="ToggleVisibilityButton_MouseLeftButtonDown" MouseLeftButtonUp="ToggleVisibilityButton_MouseLeftButtonUp" HorizontalAlignment="Right" Margin="50,-4,0,0" Foreground="Black" Background="White" Height="25" Width="25"   Kind="EyeOff" />

        <PasswordBox x:Name="txtPassword" Background="Transparent"   HorizontalAlignment="Left" PasswordChanged="txtPassword_PasswordChanged" Width="270" FontSize="14" FontFamily="Arial Unicode MS Regular">
            <PasswordBox.BorderThickness>
                <Thickness />
            </PasswordBox.BorderThickness>
           
            <PasswordBox.ToolTip>
                <ToolTip Content="Enter a valid Password"/>
            </PasswordBox.ToolTip>
        </PasswordBox>
        <TextBox x:Name="PasswordVisibletextBox"  Width="270" HorizontalAlignment="Left"
             FontSize="14" Background="Transparent"
            
             Visibility="Collapsed" BorderBrush="Transparent"
             />
    </Grid>
</UserControl>
