<UserControl x:Class="LCPReportingSystem.UserControls.ManageUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:resrc="clr-namespace:LCPReportingSystem.Properties"
             xmlns:local="clr-namespace:LCPReportingSystem"
             xmlns:usercontrol="clr-namespace:LCPReportingSystem.UserControls" xmlns:materialdesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="795" d:DesignWidth="1500">
    <UserControl.Resources>
        <Style x:Key="ScrollThumbs" TargetType="{x:Type Thumb}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Grid x:Name="Grid">
                            <Rectangle HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Width="Auto"  Height="Auto" Fill="Transparent"/>
                            <Border x:Name="Rectangle1" CornerRadius="0 0 0 0" HorizontalAlignment="Stretch"  VerticalAlignment="Stretch" Width="Auto" 
                                    Height="Auto" Background="{TemplateBinding Background}"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Tag" Value="Horizontal">
                                <Setter TargetName="Rectangle1" Property="Width" Value="Auto"/>
                                <Setter TargetName="Rectangle1" Property="Height" Value="7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!--SCROLLBARS-->
        <Style x:Key="{x:Type ScrollBar}" TargetType="{x:Type ScrollBar}">
            <Setter Property="Stylus.IsFlicksEnabled" Value="False"/>
            <Setter Property="Foreground" Value="#034268"/>
            <Setter Property="Background" Value="#684268"/>
            <Setter Property="Width" Value="10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Grid x:Name="GridRoot" Width="8" Background="{x:Null}">
                            <Track x:Name="PART_Track" Grid.Row="0" IsDirectionReversed="true" Focusable="False">
                                <Track.Thumb>
                                    <Thumb x:Name="Thumb" Background="{TemplateBinding Foreground}" 
                                  Style="{DynamicResource ScrollThumbs}"/>
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton x:Name="PageUp" Command="ScrollBar.PageDownCommand" Opacity="0" Focusable="False"/>
                                </Track.IncreaseRepeatButton>
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton x:Name="PageDown" Command="ScrollBar.PageUpCommand" Opacity="0" Focusable="False"/>
                                </Track.DecreaseRepeatButton>
                            </Track>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <Trigger SourceName="Thumb" Property="IsMouseOver" Value="true">
                                <Setter Value="{DynamicResource ButtonSelectBrush}" 
                           TargetName="Thumb" Property="Background"/>
                            </Trigger>
                            <Trigger SourceName="Thumb" Property="IsDragging" Value="true">
                                <Setter Value="{DynamicResource DarkBrush}" TargetName="Thumb" Property="Background"/>
                            </Trigger>

                            <Trigger Property="IsEnabled" Value="false">
                                <Setter TargetName="Thumb" Property="Visibility" Value="Collapsed"/>
                            </Trigger>
                            <Trigger Property="Orientation" Value="Horizontal">
                                <Setter TargetName="GridRoot" Property="LayoutTransform">
                                    <Setter.Value>
                                        <RotateTransform Angle="-90"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="PART_Track" Property="LayoutTransform">
                                    <Setter.Value>
                                        <RotateTransform Angle="-90"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="Width" Value="Auto"/>
                                <Setter Property="Height" Value="12"/>
                                <Setter TargetName="Thumb" Property="Tag" Value="Horizontal"/>
                                <Setter TargetName="PageDown" Property="Command" Value="ScrollBar.PageLeftCommand"/>
                                <Setter TargetName="PageUp" Property="Command" Value="ScrollBar.PageRightCommand"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="270"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Grid.Column="0" Grid.Row="0" Width="270" Orientation="Horizontal" HorizontalAlignment="left" Margin="0,0,0,0"   Background="#0064C1" VerticalAlignment="Center">
            <materialdesign:PackIcon Foreground="YellowGreen" Visibility="Collapsed" Height="30" Width="30" Kind="UserAlert" />
            <TextBlock Text="USER MANAGEMENT" FontWeight="SemiBold" Grid.Row="0" Padding="40,0,0,0" FontSize="16" Height="30" Foreground="White" Margin="12,5,0,0" FontFamily="Arial" ></TextBlock>

        </StackPanel>
        <TextBlock x:Name="txtSelectedUser" Grid.Row="0" Grid.Column="1" Text="" FontWeight="SemiBold" Width="200" FontSize="16" Foreground="#0064C1" Margin="20,5,0,0" FontFamily="Arial" ></TextBlock>

        <Button x:Name="BtnCreateUser" Grid.Row="0" Grid.Column="1" Width="100" Height="25" FontFamily="Arial" FontWeight="SemiBold" HorizontalAlignment="Right" Foreground="White" Content="Create User" Margin="0,0,5,0"  Click="BtnCreateUser_Click" Background="#0064C1">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="bdr_main" Background="#0064C1" CornerRadius="6">

                                    <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="bdr_main" Property="Background" Value="YellowGreen"/>
                                    </Trigger>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter TargetName="bdr_main" Property="Background" Value="#28A745"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Button.Style>
        </Button>
        <!--<ScrollViewer  Grid.Column="0" Grid.Row="1" VerticalAlignment="Top" Background="#0064C1">-->
            <ListBox x:Name="UserNameListBox" ItemsSource="{Binding Path=manageUser.ManageUserCollections}" Width="270"                             
                 Grid.Column="0" Grid.Row="1" Background="#0064c1" SelectionChanged="UserNameListBox_SelectionChanged">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="10"/>
                      
                        <Setter Property="FontSize" Value="16"/>
                    <Setter Property="Height" Value="40"/>
                    <Setter Property="FontFamily" Value="Arial"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="Background" Value="#0064c1"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="White"/>
                        <Setter Property="HorizontalContentAlignment" Value="Left"/>
                        <Setter Property="VerticalContentAlignment" Value="Center"/>

                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Border x:Name="Bd" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                SnapsToDevicePixels="true">
                                    <ContentPresenter 
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="YellowGreen"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="YellowGreen"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
                </ListBox.ItemContainerStyle>

                <ListBox.ItemTemplate>
                    <DataTemplate>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,2">
                        <materialdesign:PackIcon Kind="UserCircle" 
                                         Width="24" Height="24" 
                                         Foreground="YellowGreen"
                                         VerticalAlignment="Center"
                                         Margin="0,0,8,0"/>
                        <TextBlock Text="{Binding Username}" />
                    </StackPanel>
                </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

        <!--</ScrollViewer>-->

        <StackPanel Grid.Column="1" Grid.Row="1">

            <Border x:Name="UserdataBorder" Background="White" Visibility="Collapsed" BorderBrush="Black" Margin="2" BorderThickness="1" CornerRadius="0">
                
                <StackPanel x:Name="UserDetailPanel" Margin="4"   Orientation="Vertical">
                    <!--<materialdesign:PackIcon Foreground="YellowGreen" Height="30" Width="30" Kind="AccountPlus" />-->

                    <!-- Username -->
                    <TextBlock Text="Username:" FontWeight="SemiBold" FontSize="14" Foreground="Black" Margin="0,5,0,0"/>
                    <TextBox Text="" FontSize="14" x:Name="UsernameTextBox" IsEnabled="False" Margin="0,0,0,10" BorderBrush="#999" Height="25"  BorderThickness="1">
                        <TextBox.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="6"/>
                            </Style>
                        </TextBox.Resources>
                    </TextBox>

                    <!-- Password -->
                    <TextBlock Text="Password:" FontWeight="SemiBold" FontSize="14" Foreground="Black"/>
                    <TextBox Text="" x:Name="PasswordTextBox" FontSize="14" IsEnabled="False" Margin="0,0,0,10" Height="25" BorderBrush="#999" BorderThickness="1">
                        <TextBox.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="6"/>
                            </Style>
                        </TextBox.Resources>
                    </TextBox>

                    <!-- Email -->
                    <TextBlock Text="Email:" FontWeight="SemiBold" FontSize="14" Foreground="Black"/>
                    <TextBox Text="" FontSize="14" x:Name="EmailTextBox" IsEnabled="False" Margin="0,0,0,10" BorderBrush="#999" Height="25" BorderThickness="1">
                        <TextBox.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="6"/>
                            </Style>
                        </TextBox.Resources>
                    </TextBox>

                    <!-- User Type -->
                    <TextBlock Text="User Type:" FontWeight="SemiBold" FontSize="14" Foreground="Black"/>
                    <ComboBox Text="" FontSize="14" x:Name="UsertypeTextBox" IsEnabled="False" Margin="0,0,0,10" BorderBrush="#999"  Background="Transparent"  Height="25" BorderThickness="1">
                        
                        <ComboBox.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="6"/>
                                <Setter Property="Background" Value="Transparent"/>
                            </Style>
                        </ComboBox.Resources>
                        <ComboBoxItem>User</ComboBoxItem>
                        <ComboBoxItem>Admin</ComboBoxItem>
                    </ComboBox>

                    <!-- Is Active -->
                    <TextBlock Text="Is Active:" FontWeight="SemiBold" FontSize="14" Foreground="Black"/>
                    <TextBox Text="" FontSize="14" x:Name="IsActiveTextBox" IsEnabled="False" Margin="0,0,0,10" BorderBrush="#999" Height="25" BorderThickness="1">
                        <TextBox.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="6"/>
                            </Style>
                        </TextBox.Resources>
                    </TextBox>

                    <!-- Creation Date -->
                    <TextBlock Text="Creation Date:" FontWeight="SemiBold" FontSize="14" Foreground="Black"/>
                    <TextBox Text="" FontSize="14" x:Name="CreationDateTextBox" IsEnabled="False" Margin="0,0,0,20" BorderBrush="#999" Height="25" BorderThickness="1">
                        <TextBox.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="6"/>
                            </Style>
                        </TextBox.Resources>
                    </TextBox>

                    <!-- Buttons -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Height="40">
                        <Button x:Name="EditButton" Content="Edit" Width="80" Foreground="White" FontFamily="Arial" FontWeight="SemiBold" Margin="5" Click="EditButton_Click">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border x:Name="bdr_main" Background="#0064C1" CornerRadius="6">
                                                    
                                                    <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="bdr_main" Property="Background" Value="YellowGreen"/>
                                                    </Trigger>
                                                    <Trigger Property="IsPressed" Value="True">
                                                        <Setter TargetName="bdr_main" Property="Background" Value="#28A745"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                        </Button>
                        <Button Content="Save" Width="80" Foreground="White" FontFamily="Arial" FontWeight="SemiBold" Margin="5" Click="SaveButton_Click" >
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border x:Name="bdr_main" Background="#0064C1" CornerRadius="6">

                                                    <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="bdr_main" Property="Background" Value="YellowGreen"/>
                                                    </Trigger>
                                                    <Trigger Property="IsPressed" Value="True">
                                                        <Setter TargetName="bdr_main" Property="Background" Value="#28A745"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                        </Button>
                        <Button Content="Delete" Width="80" Foreground="White" FontFamily="Arial" FontWeight="SemiBold" Margin="5" Click="DeleteButton_Click" >
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border x:Name="bdr_main" Background="#0064C1" CornerRadius="6">

                                                    <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="bdr_main" Property="Background" Value="Red"/>
                                                    </Trigger>
                                                    <Trigger Property="IsPressed" Value="True">
                                                        <Setter TargetName="bdr_main" Property="Background" Value="#28A745"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                        </Button>
                        <Button Command="{Binding DeleteUserCommand}" Visibility="Collapsed" Style="{StaticResource btnDeleteDefault}"/>
                        
                    </StackPanel>
                </StackPanel>
            </Border>


            <Grid Visibility="Collapsed">
                
                <StackPanel Grid.Column="1"  Width="150"   VerticalAlignment="Center" Margin="-20,0,0,0">
                    <Button Command="{Binding DeleteUserCommand}"  Style="{StaticResource btnDeleteDefault}"/>
                </StackPanel>
                <StackPanel Grid.Column="1"  VerticalAlignment="Center" HorizontalAlignment="Left"   Margin="60,0,0,0" Height="35" Width="145">
                    <Button Content="Create User" Height="33"
                    Style="{StaticResource btnNewuser}"
                    Command="{Binding OpenPopupCommand}" Background="#0064C1"/>
                </StackPanel>
            </Grid>
            <StackPanel Margin="0 1 0 0" Grid.Column="1" Visibility="Collapsed">
                <ScrollViewer Height="730" Visibility="Visible" VerticalScrollBarVisibility="Auto">
                    <DataGrid x:Name="UserDataGrid" AutoGenerateColumns="False" 
                                  ItemsSource="{Binding Path=manageUser.ManageUserCollection}" IsReadOnly="False" 
                                  BorderThickness="0" HeadersVisibility="Column" CellStyle="{StaticResource DataGridCellStyle}"
                                  FontSize="16"   SelectionMode="Extended" Margin="5,5,5,5"  Background="Transparent" >
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Header="Select User" HeaderStyle="{StaticResource SubSysConfigGridHeader}">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <CheckBox IsChecked="{Binding Path=IsSelected,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}">
                                            <CheckBox.RenderTransform>
                                                <TransformGroup>
                                                    <TranslateTransform X="35" Y="0"/>
                                                </TransformGroup>
                                            </CheckBox.RenderTransform>
                                        </CheckBox>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="User Id" Binding="{Binding Userid}" IsReadOnly="True" Width="auto"  HeaderStyle="{StaticResource SubSysConfigGridHeader}"/>
                            <DataGridTextColumn Header="User Name" Binding="{Binding Username}" IsReadOnly="True" Width="*"  HeaderStyle="{StaticResource SubSysConfigGridHeader}"/>
                            <DataGridTextColumn Header="Password" Binding="{Binding Password}" IsReadOnly="True" Width="*"  HeaderStyle="{StaticResource SubSysConfigGridHeader}"/>
                            <DataGridTextColumn Header="User Type" Binding="{Binding UserType}" IsReadOnly="True" Width="auto" HeaderStyle="{StaticResource SubSysConfigGridHeader}"/>
                            <DataGridTextColumn Header="Email" Binding="{Binding Email}" IsReadOnly="True" Width="*" HeaderStyle="{StaticResource SubSysConfigGridHeader}"/>
                            <DataGridTextColumn Header="IsActive" Binding="{Binding Isactive}" IsReadOnly="True" Width="auto" HeaderStyle="{StaticResource SubSysConfigGridHeader}"/>
                            <DataGridTextColumn Header="Creation Date" Binding="{Binding Creationdate}" IsReadOnly="True" Width="*" HeaderStyle="{StaticResource SubSysConfigGridHeader}"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </ScrollViewer>
            </StackPanel>
        </StackPanel>
        <Border x:Name="CreateUserBorder" Visibility="Collapsed" Grid.Column="1" Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" Background="White" BorderBrush="Black" BorderThickness="1" CornerRadius="5" Padding="10">
            <StackPanel Margin="0,-20,0,0">
                <Border  HorizontalAlignment="Center" BorderBrush="#0064c1" BorderThickness="0,0,0,2">
                    <!--<materialdesign:PackIcon Foreground="YellowGreen" Margin="0,12,0,0" Height="30" Width="30" Kind="AccountPlusOutline" />-->

                    <TextBlock Text ="ADD NEW USER" FontSize="24" Foreground="#0064C1" Margin="7,18,0,0" Style="{StaticResource newUserRightHederText}"/>
                </Border>
               
                <StackPanel Margin="-10 12 0 0">
                    <StackPanel Style="{StaticResource newUserInputFiels}">
                        <Border Margin="0 10 0 10">
                            <TextBlock Text="{x:Static resrc:Resources.String1}" Foreground="Black" FontSize="14" FontFamily="Arial Unicode MS Regular"/>
                        </Border>
                        <Border BorderThickness="1" BorderBrush="Black" CornerRadius="5.25" Padding="5 5" Background="White">
                            <TextBox x:Name="txtNewUsername" Text="" FontSize="14" FontFamily="Arial Unicode MS Regular" Foreground="Black" Background="Transparent">
                                <TextBox.BorderThickness>
                                    <Thickness/>
                                </TextBox.BorderThickness>
                                <TextBox.ToolTip>
                                    <ToolTip Content="Enter a valid User Name "/>
                                </TextBox.ToolTip>
                            </TextBox>
                        </Border>
                    </StackPanel>
                    <StackPanel Style="{StaticResource newUserInputFiels}">
                        <Border Margin="0 10 0 10">
                            <TextBlock Text="{x:Static resrc:Resources.String10}" Foreground="Black" FontSize="14" FontFamily="Arial Unicode MS Regular"/>
                        </Border>
                        <Border BorderThickness="1" BorderBrush="Black" CornerRadius="5.25" Padding="5 5" Background="White">
                            <TextBox x:Name="txtNewMail" Text="" FontSize="14" FontFamily="Arial Unicode MS Regular" Foreground="Black" Background="Transparent">
                                <TextBox.BorderThickness>
                                    <Thickness/>
                                </TextBox.BorderThickness>
                                <TextBox.ToolTip>
                                    <ToolTip Content="Enter a valid Email Id "/>
                                </TextBox.ToolTip>
                            </TextBox>
                        </Border>
                    </StackPanel>
                    <StackPanel Style="{StaticResource newUserInputFiels}">
                        <Border Margin="0 10 0 10">
                            <TextBlock Text="{x:Static resrc:Resources.String11}" Foreground="Black" FontSize="14" FontFamily="Arial Unicode MS Regular"/>
                        </Border>
                        <Border BorderThickness="0" BorderBrush="Black" CornerRadius="5.25" Padding="5 5" Background="White">
                            <!--<ContentControl>
                                <usercontrol:PasswordUserControl x:Name="txtNewPassword" UserPassword=""/>
                            </ContentControl>-->
                            <StackPanel Height="28" Orientation="Horizontal">
                                <Border BorderBrush="Black" BorderThickness="1" CornerRadius="5">
                                    <StackPanel Width="350">

                                        <PasswordBox Height="28" BorderBrush="Transparent" x:Name="txtNewPassword" Padding="0,5,0,0" PasswordChanged="txtNewPassword_PasswordChanged"></PasswordBox>
                                        <TextBox Height="28" x:Name="PasswordVisibletextBoxSuper" FontSize="14" Padding="0,2,0,0" Visibility="Collapsed"></TextBox>

                                    </StackPanel>
                                </Border>
                                <materialdesign:PackIcon x:Name="eyeIconSuper" MouseLeftButtonDown="eyeIconSuper_MouseLeftButtonDown"  MouseLeftButtonUp="eyeIconSuper_MouseLeftButtonUp"  HorizontalAlignment="Right" Margin="2,2,0,0" Foreground="Black" Background="White" Height="25" Width="25"   Kind="EyeOff" />

                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <StackPanel x:Name="PasswordRulesPanelSuper" Margin="20,10,0,0" Visibility="Collapsed" Width="380" Background="White">
                        <TextBlock x:Name="RuleLength" FontSize="14" Text="• At least 6 characters" Foreground="Red" Opacity="0.7"/>
                        <TextBlock x:Name="RuleLower" FontSize="14" Text="• At least one lowercase letter (a-z)" Foreground="Red" Opacity="0.7"/>
                        <TextBlock x:Name="RuleUpper" FontSize="14" Text="• At least one uppercase letter (A-Z)" Foreground="Red" Opacity="0.7"/>
                        <TextBlock x:Name="RuleDigit"  FontSize="14" Text="• At least one digit (0-9)" Foreground="Red" Opacity="0.7"/>
                        <TextBlock x:Name="RuleSpecial" FontSize="14" Text="• At least one special character (@$!%*?)" Foreground="Red" Opacity="0.7"/>
                    </StackPanel>


                    <StackPanel Style="{StaticResource newUserInputFiels}">
                        <Border Margin="0 10 0 10">
                            <TextBlock Text="{x:Static resrc:Resources.String2}" Foreground="Black" FontSize="14" FontFamily="Arial Unicode MS Regular"/>
                        </Border>
                        <Border BorderThickness="0" BorderBrush="Black" CornerRadius="5.25" Padding="5 5" Background="White">
                            <!--<ContentControl>
                                <usercontrol:PasswordUserControl x:Name="txtNewConfirmPassword" UserPassword=""/>
                            </ContentControl>-->
                     
                            <StackPanel Height="28" Orientation="Horizontal">
                                <Border BorderBrush="Black" BorderThickness="1" CornerRadius="5">
                                    <StackPanel Width="350">

                                        <PasswordBox Height="28" BorderBrush="Transparent" x:Name="txtNewConfirmPassword" Padding="0,5,0,0" PasswordChanged="txtUserpasswordSuper_PasswordChanged"></PasswordBox>
                                        <TextBox Height="28" x:Name="PasswordVisibleConfirmPassword" FontSize="14" Padding="0,2,0,0" Visibility="Collapsed"></TextBox>

                                    </StackPanel>
                                </Border>
                                <materialdesign:PackIcon x:Name="eyeIconConfirm" MouseLeftButtonDown="eyeIconConfirm_MouseLeftButtonDown"  MouseLeftButtonUp="eyeIconConfirm_MouseLeftButtonUp"  HorizontalAlignment="Right" Margin="2,2,0,0" Foreground="Black" Background="White" Height="25" Width="25"   Kind="EyeOff" />

                            </StackPanel>
                        </Border>
                    </StackPanel>
                    <StackPanel Margin="25 15 0 0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="110"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0">
                                <StackPanel>
                                    <RadioButton x:Name="RadiobuttonAdmin" GroupName="UserType" Cursor="Hand" Checked="RadiobuttonAdmin_Checked" Unchecked="RadiobuttonAdmin_Unchecked"  BorderBrush="#3479C2">
                                        <RadioButton.BorderThickness>
                                            <Thickness Left="4" Right="4" Top="4" Bottom="4"/>
                                        </RadioButton.BorderThickness>
                                        <TextBlock Text="{x:Static resrc:Resources.String6}" Padding="0,3,0,0" Foreground="Black" FontSize="14" FontFamily="Arial Unicode MS Regular"  HorizontalAlignment="Center"/>
                                        <RadioButton.ToolTip>
                                            <ToolTip Content="Select Admin User option "/>
                                        </RadioButton.ToolTip>
                                    </RadioButton>
                                </StackPanel>
                            </Border>
                            <Border Grid.Column="1">
                                <StackPanel>
                                    <RadioButton x:Name="RadiobuttonUser" GroupName="UserType" Cursor="Hand" Checked="RadiobuttonUser_Checked" Unchecked="RadiobuttonUser_Unchecked" BorderBrush="#3479C2">
                                        <RadioButton.BorderThickness>
                                            <Thickness Left="4" Right="4" Top="4" Bottom="4"/>
                                        </RadioButton.BorderThickness>
                                        <TextBlock Text="{x:Static resrc:Resources.String7}" Padding="0,3,0,0" Foreground="Black" FontSize="14" FontFamily="Arial Unicode MS Regular" HorizontalAlignment="Center"/>
                                        <RadioButton.ToolTip>
                                            <ToolTip Content="Select Normal User Creation option "/>
                                        </RadioButton.ToolTip>
                                    </RadioButton>
                                </StackPanel>
                            </Border>
                        </Grid>
                    </StackPanel>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="25 15 0 0">

                    <Button x:Name="btnNewUserCreate" Width="80" Height="30" Foreground="White" FontFamily="Arial" FontWeight="SemiBold"  Content="{x:Static resrc:Resources.String8}" Click="btnNewUserCreate_Click" >
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="bdr_main" Background="#0064C1" CornerRadius="6">

                                                <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="bdr_main" Property="Background" Value="YellowGreen"/>
                                                </Trigger>
                                                <Trigger Property="IsPressed" Value="True">
                                                    <Setter TargetName="bdr_main" Property="Background" Value="#28A745"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>

                    </Button>

                    <Button  Content="Cancel" Margin="10,0,0,0" Width="80"  Height="30" Foreground="White" FontFamily="Arial" FontWeight="SemiBold" Click="Button_Click" >
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="bdr_main" Background="#0064C1" CornerRadius="6">

                                                <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="bdr_main" Property="Background" Value="Red"/>
                                                </Trigger>
                                                <Trigger Property="IsPressed" Value="True">
                                                    <Setter TargetName="bdr_main" Property="Background" Value="#28A745"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </StackPanel>
        </Border>

    </Grid>
</UserControl>
